package michael.math;

public class QuadraticFunction {
    
    /**
     * Create a QuadraticFunction object using parameter a, b, and c to repsent a*sqr(x)+b*x+c.
     * 
     * @param a
     * @param b
     * @param c
     */
    public QuadraticFunction(int a, int b, int c) {
        // TODO
    }

    /**
     * Returns how many real number roots does this Quadratic function have.  It could be 0, 1, or 2.
     * @return
     */
    public int rootCount() {
        // TODO
        return -1;
    }
    
    /**
     * Returns the axis of symmetry, which is the x value of the Vertex.
     * @return
     */
    public float axisOfSymmetry() {
        // TODO
        return 0;
    }
    
    /**
     * Returns true if the U graph shape is upside up, 
     * Returns false if the U graph shape is upside down.
     * 
     * @return
     */
    public boolean isUShaped() {
        // TODO 
        return false;
    }
}
